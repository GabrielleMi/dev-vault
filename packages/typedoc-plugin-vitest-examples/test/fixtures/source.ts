/**
 * @examplesFromTests ./test-file.ts
 */
export function addNumbers(a: number, b: number): number {
  return a + b;
}

/**
 * @examplesFromTests ./test-file.ts
 */
export function concatStrings(a: string, b: string): string {
  return `${a}${b}`;
}
